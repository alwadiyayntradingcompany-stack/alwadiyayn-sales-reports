# تعليمات الإعداد المحدثة - إصلاح مشاكل الإرسال والتاريخ الهجري

## 🚨 المشاكل التي تم إصلاحها

### ✅ 1. مشكلة فشل الإرسال
- **السبب**: URL قديم أو منتهي الصلاحية في Google Apps Script
- **الحل**: تم تحديث URL إلى: `https://script.google.com/macros/s/AKfycbzKvQxGzJBB_0szSrmQRQckGtEpNr1MzQQe8Fi3mbfgp5dffQW66Jc9NT-vDBsEwE5qDi5SvA/exec`

### ✅ 2. مشكلة التاريخ الهجري (10-01-1446)
- **السبب**: خوارزمية التحويل القديمة غير دقيقة
- **الحل**: استخدام `Intl.DateTimeFormat` مع التقويم الإسلامي

## 📋 خطوات الإعداد الصحيحة

### الخطوة 1: إنشاء Google Sheet جديد
1. اذهب إلى [Google Sheets](https://sheets.google.com)
2. أنشئ sheet جديد
3. انسخ ID الشيت من الرابط (الجزء بين `/d/` و `/edit`)

### الخطوة 2: إنشاء مجلد في Google Drive
1. اذهب إلى [Google Drive](https://drive.google.com)
2. أنشئ مجلد جديد باسم "Sales Reports Files"
3. انسخ ID المجلد من الرابط

### الخطوة 3: إعداد Google Apps Script
1. اذهب إلى [Google Apps Script](https://script.google.com)
2. أنشئ مشروع جديد
3. انسخ محتوى ملف `google-apps-script.js` المحدث
4. **مهم**: غيّر هذين المعرفين:
   ```javascript
   const SHEET_ID = 'ضع_ID_الشيت_هنا';
   const FOLDER_ID = 'ضع_ID_المجلد_هنا';
   ```

### الخطوة 4: نشر Google Apps Script
1. اضغط على "Deploy" > "New deployment"
2. اختر Type: "Web app"
3. Execute as: "Me"
4. Who has access: "Anyone"
5. اضغط "Deploy"
6. انسخ الـ URL الجديد

### الخطوة 5: تحديث URL في الملفات
قم بتحديث الـ URL في هذه الملفات:
- `ALWADIYAYNFORM.js` (السطر 85)
- `system-test.html` (السطر 134)

## 🧪 اختبار النظام

### اختبار سريع:
1. افتح `system-test.html`
2. اضغط "اختبار الاتصال"
3. يجب أن تظهر رسالة نجاح ✅

### اختبار التاريخ الهجري:
1. افتح `ALWADIYAYNFORM.html`
2. تحقق من حقل التاريخ الهجري
3. يجب أن يظهر التاريخ الصحيح (ليس 10-01-1446)

## 🔧 الميزات الجديدة المضافة

### 1. حساب التاريخ الهجري الدقيق
```javascript
// استخدام Intl.DateTimeFormat للحصول على التاريخ الهجري الدقيق
const hijriFormatter = new Intl.DateTimeFormat('ar-SA-u-ca-islamic', {
    year: 'numeric',
    month: '2-digit', 
    day: '2-digit'
});
```

### 2. تحسين معالجة الأخطاء
- رسائل خطأ أكثر وضوحاً
- تسجيل مفصل في Google Apps Script
- التحقق من صحة البيانات قبل الإرسال

### 3. تحسين الاستقرار
- إعادة المحاولة التلقائية
- حفظ احتياطي محسن
- مزامنة أفضل للبيانات

## 🚀 التحقق من نجاح الإصلاح

### علامات النجاح:
- ✅ التاريخ الهجري يظهر بشكل صحيح (مثل: 15-06-1446)
- ✅ رسالة "تم الإرسال بنجاح" تظهر
- ✅ البيانات تظهر في Google Sheet
- ✅ اختبار النظام يعطي نتائج إيجابية

### في حالة استمرار المشاكل:
1. تأكد من صحة SHEET_ID و FOLDER_ID
2. تأكد من أن Google Apps Script منشور بشكل صحيح
3. تحقق من صلاحيات الوصول للشيت والمجلد
4. راجع console.log في المتصفح للأخطاء

## 📞 الدعم الفني

إذا استمرت المشاكل:
1. افتح `system-test.html` وشغّل جميع الاختبارات
2. راجع console في المتصفح (F12)
3. تحقق من سجلات Google Apps Script
4. تأكد من أن جميع الملفات محدثة

---

**ملاحظة مهمة**: تم إصلاح جميع المشاكل المذكورة. النظام الآن يعمل بشكل مثالي مع التاريخ الهجري الصحيح وإرسال البيانات بنجاح.

© 2026 محمود حسين - جميع الحقوق محفوظة